# TEMPLATE: Replace with actual secrets using sealed-secrets or external-secrets
# DO NOT commit actual secrets to version control!
#
# To create sealed secrets:
# kubectl create secret generic securesys-secrets --dry-run=client -o yaml \
#   --from-literal=DJANGO_SECRET_KEY=<key> \
#   --from-literal=DB_USER=<user> \
#   --from-literal=DB_PASSWORD=<password> \
#   --from-literal=WEBHOOK_SECRET=<secret> | \
#   kubeseal --format yaml > sealed-secrets.yaml
#
apiVersion: v1
kind: Secret
metadata:
  name: securesys-secrets
  namespace: securesys
  labels:
    app.kubernetes.io/name: securesys-auditor
type: Opaque
stringData:
  # Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
  DJANGO_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"
  
  # Database credentials
  DB_USER: "securesys_user"
  DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  
  # Webhook signing secret
  WEBHOOK_SECRET: "CHANGE_ME_IN_PRODUCTION"
